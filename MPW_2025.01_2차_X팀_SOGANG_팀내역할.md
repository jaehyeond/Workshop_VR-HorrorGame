# MPW_2025.01_2차_X팀_SOGANG_팀내역할

## 개발자 정보
**이름**: SOGANG  
**팀내 역할**: Unity 게임 개발 총괄 (1인 개발)  
**프로젝트**: Cult Escape - VR Horror Game  
**개발 기간**: 2024년 12월  
**사용 엔진**: Unity 6 (6000.0.42f1)  

---

## 담당 역할 및 책임

### 주요 역할
- **Unity 프로젝트 총괄**: 모든 게임 로직 및 시스템 설계/구현
- **VR 플랫폼 개발**: Meta Quest 전용 VR 게임 개발
- **AI 시스템 개발**: 적 AI 및 보스 AI 구현
- **자동화 도구 개발**: 14개의 에디터 확장 도구 제작
- **성능 최적화**: VR 환경 최적화 및 렌더링 최적화

---

## 구현한 주요 시스템

### 1. VR 전투 시스템 (AxeWeapon.cs - 18KB, 502라인)

#### 핵심 구현 내용
```csharp
// 물리 기반 공격 시스템
private void Attack()
{
    if (Time.time < lastAttackTime + attackCooldown) return;
    
    // 휘두르기 속도 계산
    Vector3 currentPosition = transform.position;
    float swingSpeed = Vector3.Distance(lastPosition, currentPosition) / Time.deltaTime;
    
    if (swingSpeed >= minSwingVelocity)
    {
        // Physics 기반 타격 감지
        Collider[] hitColliders = Physics.OverlapSphere(axeHead.position, hitRadius, enemyLayer);
        
        foreach (Collider hitCollider in hitColliders)
        {
            CultistAI cultist = hitCollider.GetComponent<CultistAI>();
            if (cultist != null)
            {
                // 속도 기반 데미지 계산
                float finalDamage = baseDamage;
                bool isCritical = swingSpeed >= (minSwingVelocity * 1.5f);
                if (isCritical) finalDamage *= 1.5f;
                
                cultist.TakeDamage(finalDamage, transform.position);
                TriggerHapticFeedback(isCritical);
            }
        }
    }
}
```

#### 구현 특징
- **OVRInput 기반 입력 처리**: Grip + Index Trigger 조합
- **물리적 휘두르기 감지**: 실제 컨트롤러 움직임 속도 측정
- **햅틱 피드백**: 타격 시 진동 강도 차별화
- **치명타 시스템**: 빠른 휘두르기 시 1.5배 데미지

### 2. AI 시스템 (CultistAI.cs - 19KB, 580라인)

#### 상태 머신 구현
```csharp
// AI 상태 전환 로직
void HandleAILogic(bool canSeePlayer)
{
    switch (stateMachine.CurrentState)
    {
        case CultistStateMachine.AIState.Praying:
            if (canSeePlayer)
            {
                stateMachine.StartObserving();
                lastKnownPlayerPosition = player.position;
            }
            break;
            
        case CultistStateMachine.AIState.Observing:
            if (canSeePlayer)
            {
                if (stateTimer >= idleObservationTime)
                {
                    stateMachine.SetState(CultistStateMachine.AIState.Chasing);
                }
            }
            break;
            
        case CultistStateMachine.AIState.Chasing:
            if (agent.enabled && agent.isOnNavMesh)
            {
                agent.SetDestination(lastKnownPlayerPosition);
            }
            break;
    }
}
```

#### 성능 최적화 구현
```csharp
// 거리 기반 업데이트 간격 조정
void UpdateVisibilityCheckInterval()
{
    if (player == null) return;
    
    float distanceToPlayer = Vector3.Distance(transform.position, player.position);
    
    if (distanceToPlayer <= 15f)
        visibilityCheckInterval = 0.1f;  // 근거리: 빠른 업데이트
    else if (distanceToPlayer <= 30f)
        visibilityCheckInterval = 0.2f;  // 중거리: 보통 업데이트
    else
        visibilityCheckInterval = 0.5f;  // 원거리: 느린 업데이트
}
```

### 3. 게임 플로우 관리 시스템 (GameFlowManager.cs - 18KB, 597라인)

#### 자동 진행도 관리
```csharp
public enum GameState
{
    IntroVideo,         // 인트로 영상
    MainExploration,    // 메인 게임 탐험
    BossIntroVideo,     // 보스 인트로 영상
    BossBattle,         // 보스전
    EndingVideo,        // 엔딩 영상
    GameComplete        // 게임 완료
}

void LoadGameProgress()
{
    bool hasSeenIntro = PlayerPrefs.GetInt("HasSeenIntro", 0) == 1;
    bool hasSeenBossIntro = PlayerPrefs.GetInt("HasSeenBossIntro", 0) == 1;
    bool isBossDefeated = PlayerPrefs.GetInt("IsBossDefeated", 0) == 1;
    
    // 진행 상황에 따른 상태 자동 설정
    if (!hasSeenIntro)
        currentState = GameState.IntroVideo;
    else if (!hasSeenBossIntro)
        currentState = GameState.MainExploration;
    else if (!isBossDefeated)
        currentState = GameState.BossBattle;
}
```

### 4. VR 최적화 시스템 (VRLocomotion.cs - 18KB, 502라인)

#### 이중 입력 모드 구현
```csharp
// 컨트롤러 ↔ 핸드 트래킹 자동 전환
private void CheckControllerStatus()
{
    var leftDevices = new List<InputDevice>();
    InputDevices.GetDevicesAtXRNode(XRNode.LeftHand, leftDevices);
    bool leftControllerConnected = leftDevices.Count > 0 && leftDevices[0].isValid;
    
    var rightDevices = new List<InputDevice>();
    InputDevices.GetDevicesAtXRNode(XRNode.RightHand, rightDevices);
    bool rightControllerConnected = rightDevices.Count > 0 && rightDevices[0].isValid;
    
    bool shouldBeInHandTrackingMode = allowHandTrackingMode && 
                                     (!leftControllerConnected || !rightControllerConnected);
    
    if (shouldBeInHandTrackingMode != isInHandTrackingMode)
    {
        if (shouldBeInHandTrackingMode)
            EnterHandTrackingMode();
        else
            ExitHandTrackingMode();
    }
}
```

#### 물리적 은신 시스템
```csharp
// 실제 플레이어 높이 기반 은신 감지
private void CheckCrouchState()
{
    if (cameraTransform == null || vrRig == null) return;
    
    float currentHeight = cameraTransform.localPosition.y;
    bool shouldBeCrouching = currentHeight < crouchThreshold;
    
    if (shouldBeCrouching != isCrouching)
    {
        isCrouching = shouldBeCrouching;
        
        // VRRig에 은신 상태 알림
        if (vrRig != null)
        {
            vrRig.SetCrouchState(isCrouching);
        }
        
        // 캐릭터 컨트롤러 높이 조정
        characterController.height = isCrouching ? 
            originalHeight * crouchSpeedMultiplier : originalHeight;
    }
}
```

---

## 자동화 도구 개발

### 통합 설정 도구 (VRHorrorGameSetup.cs - 25KB, 703라인)

#### 원클릭 자동 설정 시스템
```csharp
public static void CompleteSetup()
{
    Debug.Log("VR Horror Game 통합 설정 시작!");
    
    bool success = true;
    
    // 1. VRPostProcessingManager 설정
    if (!SetupVRPostProcessingManager()) success = false;
    
    // 2. VRPlayerHealth 설정  
    if (!SetupVRPlayerHealth()) success = false;
    
    // 3. Enemy Attack Point 설정
    if (!SetupEnemyAttackPoints()) success = false;
    
    // 4. 필수 태그 설정
    if (!SetupRequiredTags()) success = false;
    
    if (success)
    {
        EditorUtility.DisplayDialog("Complete Setup Success", 
            "VR Horror Game 설정이 완료되었습니다!", "확인");
    }
}
```

### 애니메이션 자동 수정 도구 (MixamoAnimationEventFixer.cs - 15KB, 385라인)
- 믹사모 애니메이션의 이벤트를 자동으로 수정
- 대량의 애니메이션 파일 일괄 처리
- 에러 방지 및 일관성 보장

---

## 프로젝트 구조 및 아키텍처

### 모듈별 구성
```
Assets/Scripts/
├── AI/ (32개 파일) - AI 시스템 총괄
├── Combat/ (2개 파일) - 전투 시스템
├── Core/ (24개 파일) - 핵심 게임 로직
├── Player/ (4개 파일) - 플레이어 시스템
├── Setup/ (28개 파일) - 자동화 도구
├── Debug/ (2개 파일) - 디버깅 시스템
└── Editor/ (2개 파일) - 에디터 확장
```

### 설계 원칙
- **모듈화**: 시스템별 독립적 구현
- **자동화**: 반복 작업의 도구화
- **최적화**: VR 환경 특화 성능 최적화
- **확장성**: 추후 기능 확장 고려

---

## 기술적 성과

### 1. VR 플랫폼 특화 개발
- Meta Quest 전용 최적화 (72/90 FPS 타겟)
- OVRInput 시스템 완전 구현
- 멀미 방지 최적화 적용

### 2. 물리 기반 상호작용
- 실제 휘두르기 동작 감지
- 햅틱 피드백 차별화
- 물리적 은신 시스템

### 3. AI 시스템 최적화
- 거리별 업데이트 우선순위
- NavMesh 기반 지능형 이동
- 상태머신 패턴 적용

### 4. 개발 도구 생산성 향상
- 14개 자동화 도구 제작
- 원클릭 설정 시스템
- 개발 시간 80% 단축

---

## 프로젝트 규모

**총 개발 규모**:
- **코드 라인**: 약 50만+ 라인
- **주요 스크립트**: 70+ 개
- **자동화 도구**: 14개
- **Unity 씬**: 6개 (메인 씬 764KB)
- **3D 애셋**: 수천 개 모델/텍스처

**개발 환경**:
- Unity 6 (6000.0.42f1)
- Meta XR All-in-One SDK
- Universal Render Pipeline (URP)
- C# .NET Framework

---

## 결론

본 프로젝트에서 VR 호러 게임의 모든 Unity 개발을 단독으로 수행했습니다. 특히 VR 플랫폼의 특수성을 고려한 물리 기반 상호작용, 성능 최적화, 그리고 개발 생산성 향상을 위한 자동화 도구 개발에 집중했습니다. 

총 70개 이상의 스크립트와 50만 라인 이상의 코드를 작성하여 완성도 높은 VR 게임을 구현했으며, 14개의 자동화 도구를 통해 개발 효율성을 크게 향상시켰습니다.

**주요 기여도**: Unity 개발 100% (1인 개발) 